#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: alphabet ascii format
 *
 * Return:0 (Success)
*/

int main(void)
{
	int l = 0;

	while (l < 10)
{
		printf("%i", l);
		l++;
}
	printf("\n");
	return (0);
}
